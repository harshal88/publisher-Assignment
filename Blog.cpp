


#include<iostream>
#include"Blog.h"
#include<fstream>
#include<vector>

using namespace std;
#include<string>
using std::string;
using std::cin;
using std::cout;
using std::endl;


Blog::Blog()
{
}
void Blog::seeBlog() // display blog's data
{
       	  

}
/*
Article::Article()
{
}
void Article::showData() // display person's data
{
       	cout << " " <<endl;
	  //cout<< "title is " <<title;
	  //cout<< "author is " <<author;
          cout<<"article is "<<issue;
          cout<<endl;
}
void Article::enterData() // get person's data
{
	cin.clear();
	cin.sync();
       	cout << "Enter a Title "<<endl;
	
        getline(cin,title);
	cout << "Enter a author "<<endl;
	getline(cin,author);
	cout << "Enter content of article WITHOUT SPACES "<<endl;
	getline(cin,issue);
	//std::getline(std::cin,article);
        //istream& getline(article,500);
	//getline(cin,article);
}
*/










/*
#include<iostream>
#include<vector>
#include<cstring>
#include<fstream>
#include<keepr.h>
using namespace std;
int main(){keepr dontstop;
    vector<string> list;
    list.clear();
    
    string temp;
    ifstream infile("dic.dat");
    while(!(infile.eof())){
                           infile>>temp;temp+=" ";
                           list.push_back(temp);}
    
    for(int t=0;t<list.size();++t){
            cout<<list.at(t);
                 }
                 return 0;
                 }
                 */
